%\RequirePackage{textcomp}
\newcommand\subtitle[1]{\def\@subtitle{#1}}
\newcommand\director[1]{\def\@director{#1}}
\newcommand\proffesor[1]{\def\@proffesor{#1}}
\newif\ifcodirector
  \renewcommand\maketitle{\begin{titlepage}%
  \let\footnotesize\small
  \let\footnoterule\relax
  \vskip 80\p@
  \begin{center}%
    {\vbox {\vfil 
      \LARGE PROYECTO DE FIN DE CARRERA\par
      \@title\vskip 10em
      Documento de\par
      \@subtitle}}%
    \vfill%
    {\large
      \begin{tabular}[t]{*{3}{|c}|}%
        \textbf{Director del proyecto} & \textbf{Alumno proyectante} & \textbf{Profesor de proyectos} \ifcodirector \\
                                       &                             & \textbf{y Co-director del proyecto} \fi \\
                   \@director          &          \@author           & \@proffesor \\
      \end{tabular}\par}%
      \vskip 10em%
    {\large \@date \par}%       % Set date in \large size.
  \end{center}\par
  \end{titlepage}%
  \setcounter{footnote}{0}%
  \global\let\maketitle\relax
  \global\let\@author\@empty
  \global\let\@subtitle\@empty
  \global\let\@date\@empty
  \global\let\@title\@empty
  \global\let\title\relax
  \global\let\subtitle\relax
  \global\let\author\relax
  \global\let\date\relax
  \global\let\and\relax
}